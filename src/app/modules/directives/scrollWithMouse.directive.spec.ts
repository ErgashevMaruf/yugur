/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ScrollWithMouseDirective } from './scrollWithMouse.directive';

describe('Directive: ScrollWithMouse', () => {
  it('should create an instance', () => {
    const directive = new ScrollWithMouseDirective();
    expect(directive).toBeTruthy();
  });
});
